algoritmo sumacomb;

ent n, m: real;
sal suma: entero;

{Prec: n= N AND m = M AND M >= N >0}
const
A=0;
C="A";
D='A';
fconst;
var
i, comb: entero;
i2, comb2: real;
b1,b3,b4,b5: booleano;
fvar;


para i := (comb+i) * i hasta suma + comb hacer
    comb := i + comb;
    comb2:= i
fpara


{Post: n =N AND suma = SUMATORIO(i=1..N)(M sobre i)}
falgoritmo.
